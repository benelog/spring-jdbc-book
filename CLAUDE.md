# CLAUDE.md

## AsciiDoc 표기 지침

인라인 마크업(`*굵게*`, `_기울임_`, `` `코드` ``)의 기본형은 constrained 문법이라 여는 기호 앞이나 닫는 기호 뒤에 단어 문자가 붙으면 렌더링되지 않는다. Asciidoctor는 한글도 단어 문자로 취급하므로, 한국어 조사가 바로 이어지는 `*벡터*는`, `` `usage`다 ``는 별표와 백틱이 그대로 노출된다. 뒤에 공백이나 괄호가 오는 경우만 정상 동작한다.

- 한글이 바로 이어붙는 자리에서는 기호를 두 번 쓴 unconstrained 문법을 쓴다: `**벡터**는`, `` ``usage``다 ``, `__기울임__은`.
- URL이나 파일명 안의 밑줄은 강조가 아니므로 건드리지 않는다.
- 검증은 `book/build.sh` 실행 후 생성된 `book/build/book.html` 본문에 `*`나 백틱이 남아 있는지 확인한다.

## 원고 문체 지침 (book/*.adoc)

### 기술 스택 은유는 '레이어'로 표기

책의 뼈대가 되는 기술 스택 은유(JDBC → JdbcTemplate → 트랜잭션 추상화 → Spring Boot → Spring Data JDBC → Spring JDBC Plus)는 '층'이 아니라 **'레이어'**로 쓴다. 복합어도 마찬가지다: 위층/아래층 → 위 레이어/아래 레이어, 한 층씩 → 한 레이어씩, 층층이 → 레이어를 이루며.

- '계층'이 들어간 표준 기술 용어(데이터 액세스 계층, `DataAccessException` 계층, 웹 계층 등)는 이 규칙의 대상이 아니므로 손대지 않는다. `ch02-jdbc-template.adoc`의 "throws 선언을 층층이 반복"도 호출 계층에 대한 서술이라 예외다.
- `preface.adoc`의 "어느 레이어에서 문제가 생겨도 내려가 볼 수 있다"와 `ch10-jdbc-plus-deep.adoc` 말미의 같은 문장은 책의 처음과 끝을 잇는 수미상관 쌍이다. 고칠 때는 반드시 함께 고친다.

### 추상적 표현의 구체화

- **판단 기준**: 문장을 그 자리만 읽어도 '레이어'(또는 '방', '길' 같은 유사 추상어)의 지시 대상을 알 수 있는가. 바로 앞뒤 문맥에서 지시 대상이 복구되지 않으면 구체적인 기술명·클래스명·기능명으로 풀어 쓴다.
- 앞 문장에서 스택을 열거했거나 같은 문장에 클래스명이 명시된 경우의 '레이어'는 그대로 둔다.
- 클래스명 바로 옆에 붙은 '레이어'는 군더더기이므로 덜어낸다 (예: "`NamedParameterJdbcTemplate` 레이어에서" → "`NamedParameterJdbcTemplate`에서").
- '레이어' 위에 '방' 같은 두 번째 추상어를 겹쳐 쓰지 않는다. 은유가 두 개 이상 겹친 문장은 반드시 풀어 쓴다.
- 비유는 바로 다음 문장에서 구체적 서술로 풀릴 때만 유지한다.
- 기술 용어로 굳은 표현(데이터 액세스 계층, `DataAccessException` 계층 등)은 은유 검사 대상이 아니다.

### 줄표(—)

- 본문·제목·목차에서 쓰지 않는다. 삽입구는 괄호로, 문장 연결은 두 문장으로 나누고, "제목 — 부제"는 "제목: 부제"로 바꾼다.
- 실제 설정 파일이나 출력을 인용한 문자열과 코드 블록 안 내용은 원문 그대로 둔다.

### 은유 동사

한국어 산문을 쓴 뒤 아래 표현들을 grep으로 검사하고 대체한다.

- "무너진다": 무너지는 대상과 결과가 바로 이어서 구체적으로 설명될 때만 쓴다. 그 외에는 실제로 무엇이 어떻게 잘못되는지 쓴다. 보안 문맥은 "뚫린다"처럼 실제 사건을 가리키는 동사를 쓴다.
- "맞물린다": 쓰지 않는다. 실제 관계가 드러나게 바꾼다. 인과는 "~일수록 ~에 유리하다", 결합 효과는 "A에 B가 더해져", 근거는 "이유는 ~에 있다", 사용 관계는 "~를 ~가 어떻게 사용하는지".
- "가른다/갈린다": 원칙적으로 쓰지 않는다. 단순 분류는 "나뉜다", 정적인 역할 배정은 "구분한다", 조건 분기는 "정한다"(가능하면 대응 규칙까지 쓴다), 기준에 따라 답이 달라지는 서술은 기준별 결론을 직접 쓴다. "의견이 갈린다" 같은 굳은 관용구는 허용한다.
- "스며 있다/스며든다": "여러 곳에서 쓰인다"처럼 실제 작용으로 바꾼다.
- "흔든다", "숨어 있다" 같은 은유 동사도 실제 변화로 바꾼다("뼈대를 흔드는 변화" → "구성의 전제를 바꾸는 변화").

### 추상 명사·공간 은유

- "가볍다/무겁다": 소프트웨어 묘사에 쓰지 않는다. 메모리("수십 MB 메모리로 도는"), 크기("아티팩트가 크다"), 시간("오래 걸리는"), 절차("준비가 간단하다")처럼 기준을 명시한다.
- "지점": 굳은 기술 용어(조립 지점, 발생 지점)와 본래 의미만 허용한다. 그 외에는 부분, 곳, 대목, "어디서"로 바꾼다.
- "축": 좌표축·도표의 축과 기준 두 개를 직교시키는 진짜 2차원 구도에만 쓴다. 기준·원인·항목을 가리키는 용법은 "기준", "원인", "이 네 가지"로 바꾼다.
- "겹": 실제 층 구조나 물리적 겹침에만 쓴다. 추상 명사에 얹는 용법은 개수("네 가지가 한꺼번에")나 "더해진다"로 바꾼다.
- "손잡이": 물리적 손잡이에만 쓴다. 설정·제어 수단 비유는 무엇을 어떻게 조작하는지 쓴다("환경 변수라는 손잡이" → "환경 변수로 동작 바꾸기").
- "층위": 추상적 구분에 쓰지 않고 실제 차이 기준으로 바꾼다("여러 층위가 있다" → "규모가 다양하다"). 기술 스택 은유는 위의 '레이어' 규칙을 따른다.
- "뼈대": "기본 구조", "전체 구조", "기본 절차"처럼 무엇의 어떤 구조인지 드러나게 바꾼다.
- "틀": 가리키는 대상이 바로 앞뒤에서 구체적으로 정의될 때만 쓴다. 지시어로만 받는 "이 틀"은 실제 대상으로 바꾼다.
- "지도", "밑바탕" 같은 공간 은유는 실제 하는 일로 바꾼다. 책 전체를 관통하도록 의도한 비유는 남길 수 있지만, 은유를 겹쳐 쓰지 않는다.

### 한정·종결 표현

- "사실상": 굳은 용어("사실상 표준")이거나 정확성에 꼭 필요한 한정일 때만 쓴다. 그 외에는 삭제하거나 풀어쓴다("사실상 0" → "0에 가깝다", "사실상 필수다" → "필수가 된다").
- "~인 셈이다": 비유를 맺을 때만 쓴다. 일반 서술의 결론은 직설 종결("확장된 셈이다" → "확장됐다"). 대체 표현이 "~인 것이다"로 쏠리지 않게 한다.
