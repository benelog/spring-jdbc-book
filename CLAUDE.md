# CLAUDE.md

## 공통 글쓰기 지침은 플러그인에 있다

한국어 기술 글쓰기의 공통 문체·표기 규칙과 AsciiDoc 마크업 규칙은
`korean-tech-writing` 플러그인의 두 스킬(`korean-tech-writing`, `korean-asciidoc`)에 있다.
줄표 금지, 은유 동사와 공간 은유 대체, "사실상"·"~인 셈이다" 같은 한정·종결 표현,
번역투와 조사 호응, 외래어 표기, 인라인 마크업 뒤 한글 조사(이중 백틱) 규칙은 모두 그쪽을 따른다.
이 파일에는 그 공통 지침으로 결정되지 않는 이 저장소만의 규칙을 적는다.
아래 항목을 다른 저장소에도 통하는 일반 규칙으로 늘려 쓰지 않는다.

- 플러그인은 프로젝트 스코프로 설치되어 있다(`.claude/settings.json`).
- 어조는 문어체 평서형("~다")이다. 경어체를 섞지 않는다.

## AsciiDoc 렌더링 검증

`book/build.sh`를 실행한 뒤 생성된 `book/build/book.html` 본문에 `*`나 백틱이 그대로 남아 있는지 확인한다.

## 원고 문체 지침 (book/*.adoc)

### 기술 스택 은유는 '레이어'로 표기

책의 기본 구조가 되는 기술 스택 은유(JDBC → JdbcTemplate → 트랜잭션 추상화 → Spring Boot → Spring Data JDBC → Spring JDBC Plus)는 '층'이 아니라 **'레이어'**로 쓴다. 복합어도 마찬가지다: 위층/아래층 → 위 레이어/아래 레이어, 한 층씩 → 한 레이어씩, 층층이 → 레이어를 이루며.

- '계층'이 들어간 표준 기술 용어(데이터 액세스 계층, `DataAccessException` 계층, 웹 계층 등)는 이 규칙의 대상이 아니므로 손대지 않는다. `ch02-jdbc-template.adoc`의 "throws 선언을 층층이 반복"도 호출 계층에 대한 서술이라 예외다.
- `preface.adoc`의 "어느 레이어에서 문제가 생겨도 내려가 볼 수 있다"와 `ch10-jdbc-plus-deep.adoc` 말미의 같은 문장은 책의 처음과 끝을 잇는 수미상관 쌍이다. 고칠 때는 반드시 함께 고친다.

### '레이어'를 언제 구체적인 이름으로 푸는가

- **판단 기준**: 문장을 그 자리만 읽어도 '레이어'(또는 '방', '길' 같은 유사 추상어)의 지시 대상을 알 수 있는가. 바로 앞뒤 문맥에서 지시 대상이 복구되지 않으면 구체적인 기술명·클래스명·기능명으로 풀어 쓴다.
- 앞 문장에서 스택을 열거했거나 같은 문장에 클래스명이 명시된 경우의 '레이어'는 그대로 둔다.
- 클래스명 바로 옆에 붙은 '레이어'는 군더더기이므로 덜어낸다 (예: "`NamedParameterJdbcTemplate` 레이어에서" → "`NamedParameterJdbcTemplate`에서").
- '레이어' 위에 '방' 같은 두 번째 추상어를 겹쳐 쓰지 않는다.
- 기술 용어로 굳은 표현(데이터 액세스 계층, `DataAccessException` 계층 등)은 이 검사 대상이 아니다.
